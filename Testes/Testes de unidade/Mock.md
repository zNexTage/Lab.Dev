# Mock

Objetos simulados que simulam o comportamento de objetos reais de forma controlada.

Normalmente são criados para testar o comportamento de outros objetos. Em outras palavras, os objetos mock são "falsos" que simulam o comportamento de uma classe ou objeto "real"para que possamos focar o teste na unidade a ser testada.

